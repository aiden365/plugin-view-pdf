- [x] 设置面板支持配置股票列表（英文逗号分隔），保存后重启仍生效，并具备清洗与错误提示
- [x] 设置面板支持列选择器（下拉多选），默认列正确，修改后表格列实时变化且持久化
- [x] 底部 ToolWindow 中间栏新增 Swatch 栏目，默认隐藏，可通过顶部功能按钮显示/隐藏；栏目内以表格展示行情，至少包含代码、名称、操作栏，并支持空态与“数据不可用”
- [x] 新浪行情接口支持批量请求并正确解析多行响应，中文名称不乱码（GBK/GB2312 兜底）
- [x] 定时刷新默认 5 秒，可配置；失败时不崩溃并展示错误摘要，具备退避与恢复
- [x] 单股阈值可通过操作栏配置并持久化；未设置阈值的股票不触发通知
- [x] 触发通知规则为 |涨跌幅%| >= 阈值；通知内容包含 code、name、price、changePct、threshold、quoteDateTime、lastRefreshTime
- [x] 冷却期默认 5 分钟且可配置；同一股票冷却期内不重复通知；冷却状态不持久化
